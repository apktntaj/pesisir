{
  "name": "pesisir-api",
  "version": "1.0.0",
  "description": "Lightweight backend service for LarTas/government API communication",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
